const express = require('express');
// import middlewares from ./middlewares

const router = express.Router();

// write REST requests
// for ex: router.post('/', createMiddleware)
// for ex: router.get('/:id', getMiddleware)

module.exports = router;
